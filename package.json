{
  "name": "projeto-ts",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tsc && node ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
